server:
  port: 8082

spring:
  application:
    name: creed-resource-order
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:9000

creed:
  oauth2:
    jwk-set-cache-minutes: 10

package com.creed.resource.envmatrix.api.dto;

/** Lifecycle of a release. The topology page defaults its picker to the non-archived ones. */
public enum ReleaseStatus {
    DRAFT,
    ACTIVE,
    ARCHIVED
}

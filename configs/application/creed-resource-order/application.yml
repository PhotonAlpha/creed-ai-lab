spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/config-order
    username: root-order
    password: root-order-pwd
  profiles:
    include:
      - order
